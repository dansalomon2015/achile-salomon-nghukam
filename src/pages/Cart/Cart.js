import React, { Component } from "react";
import { Navbar } from "../../components";

export class Cart extends Component {
    render() {
        return (
            <div>
                <Navbar />
            </div>
        );
    }
}
